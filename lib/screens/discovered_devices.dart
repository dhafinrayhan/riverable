import 'package:flutter/material.dart';

class DiscoveredDevicesScreen extends StatelessWidget {
  const DiscoveredDevicesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Discovered Devices'),
      ),
      body: Container(),
    );
  }
}
